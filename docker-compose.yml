version: '3.8'

services:
  neuroquant:
    build: .
    container_name: neuroquant-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # - OPENAI_API_KEY=sk-your-key-here (Optional: Set on server)
    restart: unless-stopped